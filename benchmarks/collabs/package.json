{
  "name": "collabs",
  "version": "1.0.0",
  "description": "Collabs benchmarks",
  "main": "./run.js",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "start": "node --expose-gc run.js && npm run table",
    "table": "echo 'Collabs results: \n\n' && node ../../bin/render-table.js ../results.json 6000 collabs",
    "dist": "npm run clean && rollup -c && gzip --keep dist/*"
  },
  "author": "Matthew Weidner",
  "license": "MIT",
  "dependencies": {
    "@collabs/collabs": "0.11.0"
  }
}
